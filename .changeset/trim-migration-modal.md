---
"@paddock/server": patch
---

Trim the transcript-migration modal. The per-row state explanation ("One copy is just a longer version of the other. Lossless either way.") was the badge restated in a sentence, so it is now the badge's tooltip; the session id and sidecar count move to the row's hover text; the destination store is shown home-relative; and the "also moving" disclosure collapses to counts with the reasoning one click away. Same information, 39% fewer words and 29% less height on an identical plan.

---
---

docs: refresh the README to reflect the herdctl session runtime + token-by-token streaming (now the default drive mode), unified Triggers/automation, file & image attachments, the self-management MCP, and the YAML instance-config loader (correcting the stale "environment-only — no config files" note and the CLI-vs-SDK runtime framing). README/root-only; no package change.
